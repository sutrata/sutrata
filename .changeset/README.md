# Changesets

Every PR that changes `packages/parser/src` or `packages/editor/src` needs a
changeset. Run `pnpm changeset` and pick the packages and bump type; see
[CONTRIBUTING.md](../CONTRIBUTING.md#releases-and-changesets) for the semver
rules and the migration-note requirement for breaking changes.
